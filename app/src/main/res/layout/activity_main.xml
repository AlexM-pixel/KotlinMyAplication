<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".view.ui.Main6Activity">


    <data>

        <variable
            name="adapter"
            type="com.example.kotlinmyaplicationtasks.view.adapters.UserAdapter" />

        <variable
            name="userViewModel"
            type="com.example.kotlinmyaplicationtasks.viewModel.UsersViewModel" />

    </data>

    <LinearLayout
        android:id="@+id/bbb"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_usersId"
            android:layout_width="match_parent"
            android:layout_height="300dp"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:setAdapter="@{adapter}"
            tools:listitem="@layout/item_for_recycler" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <EditText
                android:id="@+id/name_id"
                android:layout_width="170dp"
                android:layout_height="wrap_content"
                android:layout_margin="8dp"
                android:hint="set name"
                android:text="@={userViewModel.name}"
                android:textStyle="italic" />

            <EditText
                android:id="@+id/age_id"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:inputType="number"
                android:layout_gravity="center"
                android:layout_margin="8dp"
                android:hint="set age"
                android:text="@={userViewModel.age}" />
        </LinearLayout>

        <Button
            android:id="@+id/button_add"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="12dp"
            android:onClick="@{()->userViewModel.addUser()}"
            android:paddingStart="46dp"
            android:paddingEnd="46dp"
            android:text="@string/add_button" />
    </LinearLayout>


</layout>