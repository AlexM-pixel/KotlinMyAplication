<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".view.ui.Main6Activity">


    <data>

        <variable
            name="adapter"
            type="com.example.kotlinmyaplicationtasks.view.adapters.UserAdapter" />

        <variable
            name="userViewModel"
            type="com.example.kotlinmyaplicationtasks.viewModel.UsersViewModel" />

    </data>

    <LinearLayout
        android:id="@+id/bbb"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_usersId"
            android:layout_width="match_parent"
            android:layout_height="280dp"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:setAdapter="@{adapter}"
            tools:listitem="@layout/item_for_recycler" />

        <View
            android:id="@+id/divider_first"
            android:layout_width="match_parent"
            android:layout_height="2dp"
            android:background="?android:attr/listDivider" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <EditText
                android:id="@+id/name_id"
                android:layout_width="170dp"
                android:layout_height="wrap_content"
                android:layout_margin="8dp"
                android:hint="set name"
                android:text="@={userViewModel.name}"
                android:textStyle="italic" />

            <EditText
                android:id="@+id/age_id"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:layout_margin="8dp"
                android:hint="set age"
                android:inputType="number"
                android:text="@={userViewModel.set_age}" />

            <RadioGroup
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:orientation="horizontal">

                <RadioButton
                    android:id="@+id/radioButton_man"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:checked="true"
                    android:onClick="@{(view)->userViewModel.buttonSetGender(view)}"
                    android:text="М" />

                <RadioButton
                    android:id="@+id/radio_woman"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:onClick="@{(view)->userViewModel.buttonSetGender(view)}"
                    android:text="Ж" />
            </RadioGroup>


        </LinearLayout>

        <Button
            android:id="@+id/button_add"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="12dp"
            android:onClick="@{(view)->userViewModel.addUser(view)}"
            android:paddingStart="46dp"
            android:paddingEnd="46dp"
            android:text="@string/add_button" />

        <View
            android:id="@+id/divider"
            android:layout_width="match_parent"
            android:layout_height="2dp"
            android:background="?android:attr/listDivider" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="сортировка:"
            android:textSize="28sp" />

        <EditText
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@={userViewModel.sort_age}"
            android:hint="insert age" />

        <CheckBox
            android:id="@+id/checkBoxMan"
            android:text="man"
            android:checked="@={userViewModel.man}"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <CheckBox
            android:id="@+id/checkBoxWoman"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:checked="@={userViewModel.woman}"
            android:text="woman" />
<Button
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center_horizontal"
    android:text="sort"
    android:onClick="@{()->userViewModel.setAge()}"/>

    </LinearLayout>


</layout>